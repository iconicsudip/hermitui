# Hermit-UI a component library based on tailwind.css

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Buttons

Component name

### `<HButton />`

### Properties

`label -> string`

`type -> 'default' | 'warning' | 'primary' | 'secondary' | 'info' | 'error'`

`style -> CSSProperties`

`size -> 'default' | 'large' | 'small'`

`className -> string`

